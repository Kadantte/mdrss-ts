{
	"name": "mdrss-ts",
	"license": "MPL-2.0",
	"workspaces": [
		"backend",
		"frontend"
	],
	"scripts": {
		"start": "bun run --bun --cwd backend start",
		"fdev": "bun run --bun --cwd frontend dev",
		"fbuild": "bun run --cwd frontend generate",
		"bdev": "bun run --bun --cwd backend dev",
		"migrate": "bun run --bun --cwd backend migrate"
	},
	"prettier": {
		"singleQuote": true,
		"semi": false,
		"trailingComma": "none",
		"useTabs": true,
		"arrowParens": "avoid"
	}
}
